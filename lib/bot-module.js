
// Don't know if i'll need this yet,
// but i want a base class in place

module.exports = class BotModule {
    constructor() {
        this.test = "hello world";
    }

    handleChatMessage(message, client) { }
}